print(a, b)
# comment
build.debug = True
print(first branch)
last block
for (int i = 0; i <= 5; i++) {
    out('i = ' + i);
}
// abc
if second_branch_condition:
    last block
    for (int i = 0; i <= 5; i++) {
        out('i = ' + i);
    }
    // abc
    after if and #3
if first condition:
    if second condition:
        if third condition:
            code1
            code2
            code3
